import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rondas.app',
  appName: 'Rondas ARC Simón Bolívar',
  webDir: 'dist'
};

export default config;
